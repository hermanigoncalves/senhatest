# CMIPtst — estado V1

O banco real do projeto `CMIPtst` usa UUID. O arquivo `01_cmip_v1_legacy_bigint_DO_NOT_RUN.sql` é legado e NÃO deve ser executado.

As alterações V1 foram aplicadas diretamente ao projeto `echypqclxnztvjicnkbf` e estão registradas no histórico de migrations do Supabase.

Frontend:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_PUBLISHABLE_KEY`

Nunca colocar `service_role`, secret key ou senha de banco no frontend.
