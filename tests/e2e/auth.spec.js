import { test, expect } from '@playwright/test';
const accounts=[['master','E2E_MASTER_EMAIL','E2E_MASTER_PASSWORD'],['admin','E2E_ADMIN_EMAIL','E2E_ADMIN_PASSWORD'],['receptionist','E2E_RECEPTION_EMAIL','E2E_RECEPTION_PASSWORD'],['doctor','E2E_DOCTOR_A_EMAIL','E2E_DOCTOR_A_PASSWORD']];
for(const [role,ek,pk] of accounts)test(`login ${role}`,async({page})=>{test.skip(!process.env[ek]||!process.env[pk],`Credenciais ${role} ausentes`);await page.goto('/');await page.getByPlaceholder('E-mail').fill(process.env[ek]);await page.getByPlaceholder('Senha').fill(process.env[pk]);await page.getByRole('button',{name:'Entrar'}).click();await expect(page.getByText(role,{exact:true}).first()).toBeVisible();await page.getByRole('button',{name:/Sair/}).click();});
test('primeiro acesso exige troca de senha',async({page})=>{test.skip(!process.env.E2E_FIRST_ACCESS_EMAIL||!process.env.E2E_FIRST_ACCESS_PASSWORD,'Conta de primeiro acesso ausente');await page.goto('/');await page.getByPlaceholder('E-mail').fill(process.env.E2E_FIRST_ACCESS_EMAIL);await page.getByPlaceholder('Senha').fill(process.env.E2E_FIRST_ACCESS_PASSWORD);await page.getByRole('button',{name:'Entrar'}).click();await expect(page.getByRole('heading',{name:'Primeiro acesso'})).toBeVisible();});
